# Módulos importados
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import random
import math

inputs = np.array([[0,1], [0,0], [1,1], [1,0], [0,1] ,[1,1]])

values = np.zeros(1 + inputs.shape[1])

values2 = np.random.choice([0., 1.], size=(1 + inputs.shape[1]),), p=[.5, .5])

np.random.choice([0., 1.], size=((1 + inputs.shape[1]),), p=[.5, .5])


print(values)
print(values2)

